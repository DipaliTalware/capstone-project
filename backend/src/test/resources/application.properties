# Spring Profile
spring.profiles.active=test

# MongoDB configuration for tests
spring.data.mongodb.uri=mongodb://localhost:27017/test-db
spring.data.mongodb.database=test-db

# Logging for test (optional, to reduce noise during tests)
logging.level.org.springframework=WARN
logging.level.de.flapdoodle=WARN
